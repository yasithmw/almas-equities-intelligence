// Minimal stub for Task 6 (the shell). Task 8 replaces this file in
// place with the real pre-built dashboard pinned by Exhibit C (KPI row,
// tabs, filters, panels). This exists only so the shell's routing test
// has a "Market Overview" heading to find.
export default function DashboardsPane() {
  return (
    <div style={{ padding: 24 }}>
      <h1>Market Overview</h1>
    </div>
  )
}
